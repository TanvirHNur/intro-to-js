// var var var , yeah that's the variable , you can find it in evrey language
// but the interesting thing is you can find variable in your every day life

// 

// There are some rules while declaring a JavaScript variable (also known as identifiers).

// Name must start with a letter (a to z or A to Z), underscore( _ ), or dollar( $ ) sign.
// After first letter we can use digits (0 to 9), for example value1.
// JavaScript variables are case sensitive, for example x and X are different variables.



// Let’s see a simple example of JavaScript variable.
var price = 50;
//  so do notice, what you need to write a varialbe
//  yeah, five things you need to write a var
//  1. write 'Var'
// 2. Var name
// 3. + (Equal sign)
// 4.Value
//  5. ; (semi-clone)
// Ex: var age = 22;

 
var age = 23;
var temparature = 30;
var man = 'rakiv, sakib';

    console.log(man);

var name = 'karim uddin';
    console.log(nam)    
var addrees = 'DA. FARMGATE';
var friend = 'rakib ahmad'
var school = 'tomuk govt school'

var pass = true;
var assi = false;


// A JavaScript variable is simply a name of storage location. 
// There are three types of variables in JavaScript : 

// ****Neumeric; number type: you can expose it by number
// JavaScript has only one type of number. Numbers can be written with or without decimals.
  var x = 5; //   number without decimals
 var x = 5.5;  // number with decimals


// ****String ; word type- in your life you can't expose evrey thing in number and it's in programming language 
// A JavaScript string stores a series of characters like "John Doe".
// A string can be any text inside double or single quotes:
var name = 'karim uddin';

// Boolean = ture or false it's like yes or not . if yes that's true , if no that's false
var pass = true;
var assi = false;
// Null: This type has only one value : null.
// Undefined: A variable that has not been assigned a value is undefined.
